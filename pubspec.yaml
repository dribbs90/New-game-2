name: medieval_tower_defense
description: Isometric medieval tower defense in portrait mode
version: 0.1.0
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.8.0
  flame_isometric: ^0.3.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/map/iso_map.tmx
    - assets/images/
